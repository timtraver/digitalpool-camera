[Unit]
Description=Network watchdog — reboot if every uplink is unreachable
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/network-watchdog.sh
# /var/lib/network-watchdog — last-known-good uplink + last-reboot markers
StateDirectory=network-watchdog
